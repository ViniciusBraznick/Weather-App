import React from 'react';

function Header(){
    return(
        <header>
            <div className="logo">
                <h1>Weather App</h1>
            </div>
        </header>
    );
}

export default Header;